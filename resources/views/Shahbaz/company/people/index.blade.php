@extends('layouts.app')
@php($titles=['personnel'=>'پرسنل','board'=>'هیئت‌مدیره','shareholders'=>'سهامداران'])
@section('header_title',$titles[$type])
@section('content')
<div dir="rtl" class="mx-auto max-w-6xl space-y-5"><div class="flex items-center justify-between rounded-3xl border bg-white p-6"><div><h1 class="text-xl font-black">{{ $titles[$type] }}</h1><p class="mt-2 text-sm text-slate-500">{{ $editable?'پرونده باز است؛ امکان افزودن، ویرایش و بایگانی وجود دارد.':'پرونده بسته یا در حال بررسی است؛ اطلاعات فقط قابل مشاهده است.' }}</p></div>@if($editable)<a href="{{ route('company.shahbaz.people.create',$type) }}" class="rounded-xl bg-emerald-600 px-5 py-3 font-black text-white">+ ایجاد</a>@endif</div>
@if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 font-bold text-emerald-800">{{ session('success') }}</div>@endif
<div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($items as $item)<article class="rounded-2xl border bg-white p-5 shadow-sm"><div class="flex justify-between"><b>{{ $item->person->first_name }} {{ $item->person->last_name }}</b><span class="rounded-full px-2 py-1 text-xs {{ $item->status==='archived'?'bg-violet-100':'bg-emerald-100' }}">{{ $item->status==='archived'?'بایگانی':'فعال' }}</span></div><div class="mt-4 space-y-2 text-sm"><div>کد ملی/گذرنامه: {{ $item->person->national_code ?: $item->person->passport_number }}</div><div>عنوان/سمت: {{ $item->job_title ?: ($item->board_position ?: ($item->shareholder_type ?: '---')) }}</div><div>شروع: {{ $item->started_on?->format('Y-m-d') ?: '---' }}</div><div>پایان: {{ $item->ended_on?->format('Y-m-d') ?: '---' }}</div></div>@if($editable&&$item->status!=='archived')<div class="mt-5 flex gap-3"><a class="font-bold text-blue-700" href="{{ route('company.shahbaz.people.edit',[$type,$item]) }}">ویرایش</a><form method="POST" action="{{ route('company.shahbaz.people.archive',[$type,$item]) }}">@csrf @method('DELETE')<input type="hidden" name="archive_reason" value="بایگانی توسط شرکت"><button class="font-bold text-rose-700">بایگانی</button></form></div>@endif</article>@empty<div class="rounded-2xl border border-dashed bg-white p-10 text-center text-slate-500 md:col-span-3">رکوردی ثبت نشده است.</div>@endforelse</div></div>
@endsection
