@extends('layouts.admin')
@section('header_title','کنترل پرونده شرکت')
@section('content')
<div dir="rtl" class="mx-auto max-w-6xl space-y-6"><section class="rounded-3xl border bg-white p-6"><div class="flex flex-wrap items-center justify-between gap-4"><div><h1 class="text-2xl font-black">{{ $company->name_fa }}</h1><p class="mt-2 text-sm text-slate-500">شناسه ملی: {{ $company->national_id }} — وضعیت: {{ $company->shahbaz_verification_status }}</p></div><a href="{{ route('association.shahbaz.companies.index') }}" class="rounded-xl border px-5 py-3 font-black">بازگشت به صف</a></div></section>
<section class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@foreach($sections as $section)@php($status=$section['review_status'])<article class="rounded-2xl border p-5 {{ $status==='approved'?'border-emerald-300 bg-emerald-50':($status==='correction_required'?'border-rose-300 bg-rose-50':'border-amber-300 bg-amber-50') }}"><div class="flex items-center justify-between"><h2 class="font-black">{{ $loop->iteration }}. {{ $section['label'] }}</h2><span class="rounded-full bg-white/80 px-3 py-1 text-xs font-bold">{{ $status==='approved'?'تأیید شده':($status==='correction_required'?'دارای نقص':'در انتظار کنترل') }}</span></div><p class="mt-3 text-sm text-slate-600">تعداد رکورد: {{ $section['count'] }}</p><a href="{{ route('association.shahbaz.companies.dossier.section',[$company,$section['key']]) }}" class="mt-4 inline-block font-black text-emerald-800">ورود به بخش</a></article>@endforeach</section>
<section class="rounded-2xl border border-blue-200 bg-blue-50 p-5"><a href="{{ route('association.shahbaz.companies.show',$company) }}" class="font-black text-blue-800">کنترل نهایی پرونده، استعلام شحباز و صورتحساب ←</a></section></div>
@endsection
